{
  "dependencies": {
    "socket.io": "^4.1.3"
  },
  "scripts": {
    "start": "node index.js",
    "deploy": "git push heroku `git subtree split --prefix server setup-deploy`:master --force"
  },
  "engines": {
    "node": "14.x",
    "yarn": "1.x"
  },
  "name": "juntin-server",
  "version": "1.0.0",
  "description": "The server for Juntin platform",
  "main": "index.js",
  "author": "Bruno Gomes",
  "license": "MIT"
}
